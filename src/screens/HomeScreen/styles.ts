import {StyleSheet} from 'react-native';

import {TULIP} from '../../constants/colors';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    height: '100%',
    justifyContent: 'center',
    backgroundColor: TULIP,
  },
});

export default styles;

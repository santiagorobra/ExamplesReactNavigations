import {StyleSheet} from 'react-native';

import {PARADISO} from '../../../constants/colors';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: PARADISO,
  },
});

export default styles;
